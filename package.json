{
  "name": "flyupmem",
  "version": "0.4.0",
  "description": "Local-first, zero-cost memory plugin for AI agents",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "flyupmem": "dist/index.js",
    "flyupmem-mcp": "dist/mcp/entry.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run && python3 -m unittest tests/hermes_plugin_boundary_test.py -v",
    "test:ts": "vitest run",
    "test:hermes-plugin": "python3 -m unittest tests/hermes_plugin_boundary_test.py -v",
    "test:watch": "vitest",
    "dev": "tsx src/index.ts"
  },
  "keywords": [
    "memory",
    "ai-agent",
    "local-first",
    "yaml"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@xenova/transformers": "^2.17.2",
    "js-yaml": "^4.1.0",
    "zod": "^3.23.0"
  }
}
