name: flyupmem
version: 0.5.1
description: "FlyupMem — local-first memory with YAML storage, multi-signal retrieval, ACT-R decay."
pip_dependencies: []
requires_env: []
hooks:
  - on_session_end
  - on_pre_compress
