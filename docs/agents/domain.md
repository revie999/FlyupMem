# Domain Docs

**Layout:** Single-context

One `CONTEXT.md` at the project root holds the domain language, key concepts, and terminology for the entire project. One `docs/adr/` directory holds Architecture Decision Records.

## Structure

- `CONTEXT.md` — Project domain language (entities, relationships, conventions)
- `docs/adr/` — Architecture Decision Records (numbered, one per file)
